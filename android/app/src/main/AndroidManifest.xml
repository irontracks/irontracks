<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <receiver
            android:name=".RestTimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.irontracks.REST_TIMER_FIRE" />
                <action android:name="com.irontracks.APP_NOTIFICATION" />
                <action android:name="com.irontracks.REST_TIMER_CANCEL" />
            </intent-filter>
        </receiver>

        <!-- Rest timer foreground service (Android 8+). Exempt from Doze/Standby so
             rest timers fire precisely even when the screen is off mid-workout. -->
        <service
            android:name=".RestTimerService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Sentry Android — auto-init via meta-data. When SENTRY_DSN placeholder
             is empty (no build property set) the SDK stays in no-op mode and
             does NOT send events, so this is safe in dev/local builds. -->
        <meta-data android:name="io.sentry.dsn" android:value="${SENTRY_DSN}" />
        <meta-data android:name="io.sentry.environment" android:value="production" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="0.1" />
        <meta-data android:name="io.sentry.send-default-pii" android:value="false" />
        <meta-data android:name="io.sentry.auto-init" android:value="true" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Foreground Service for rest timer (Android 8+). FOREGROUND_SERVICE_HEALTH
         requires API 34+; the service type "health" matches the fitness use case
         and avoids the stricter media/location categories. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Location (GPS): required for Cardio route tracking and gym check-in
         via @capacitor/geolocation. Without these, requestPermissions() on
         Android silently returns denied and GPS never starts — which is
         exactly the bug that was preventing Cardio GPS from working on
         Android. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>

