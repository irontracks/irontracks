<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions must be manifest children before <application>. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- User-granted special access used only as a legacy timer fallback. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Camera and microphone features are optional: devices without either one
         can still install and use the rest of IronTracks. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Foreground services: rest timer (specialUse) and cardio GPS (location). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- No background-location permission: the GPS service is started by the user
         while the app is visible and remains as a location foreground service. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <receiver
            android:name=".RestTimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.irontracks.REST_TIMER_FIRE" />
                <action android:name="com.irontracks.APP_NOTIFICATION" />
                <action android:name="com.irontracks.REST_TIMER_CANCEL" />
            </intent-filter>
        </receiver>

        <!-- Rest timer foreground service (Android 8+): só mantém a notificação de
             contagem visível. O disparo real do fim do descanso é sempre agendado via
             AlarmManager.setExactAndAllowWhileIdle (não depende do sucesso do FGS).
             Tipo "specialUse" (não "health") porque health exige permissão de sensor em
             runtime no Android 14+ e o startForeground lançava SecurityException. -->
        <service
            android:name=".RestTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="rest_timer_countdown" />
        </service>

        <!-- Cardio GPS foreground service: mantém o FusedLocationProvider entregando
             posições em background (tela bloqueada/celular no bolso) durante a corrida,
             espelhando o CLLocationManager em background do iOS. -->
        <service
            android:name=".CardioLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Sentry Android — auto-init via meta-data. When SENTRY_DSN placeholder
             is empty (no build property set) the SDK stays in no-op mode and
             does NOT send events, so this is safe in dev/local builds. -->
        <meta-data android:name="io.sentry.dsn" android:value="${SENTRY_DSN}" />
        <meta-data android:name="io.sentry.environment" android:value="production" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="0.1" />
        <meta-data android:name="io.sentry.send-default-pii" android:value="false" />
        <meta-data android:name="io.sentry.auto-init" android:value="true" />

        <!-- Ícone monocromático da notificação push (FCM). Sem isto o Android 8+ mostra
             um quadrado branco (o launcher opaco vira silhueta alpha). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />
    </application>

</manifest>
