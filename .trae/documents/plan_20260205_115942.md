Vou alterar o componente `MuscleMapCard` para que o modo de visualização padrão seja "Semana" em vez de "Dia".

**Passos:**
1.  Editar `src/components/dashboard/MuscleMapCard.tsx`.
2.  Adicionar uma propriedade opcional `defaultViewMode` na interface `Props` para permitir flexibilidade futura.
3.  Alterar a inicialização do estado `period` para usar essa prop ou assumir `'week'` (Semana) como padrão.

Isso garantirá que, ao abrir o dashboard, o mapa muscular mostre a visão semanal automaticamente.