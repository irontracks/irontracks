## Contexto (o que a Apple pediu)
- **Guideline 4.8 (Login Services):** se existe login social/terceiros, precisa oferecer **Sign in with Apple** como opção equivalente.
- **Guideline 2.1 (Information Needed):** o revisor não pode ficar travado em etapas que exigem “código”/aprovação; precisamos garantir acesso completo com a conta demo.

## Premissas (com base no que você disse)
- O provedor **Apple já está configurado no Supabase**.
- Você vai usar a conta demo **apple-test@irontracks.com.br / 123456** nas notas de revisão.

## Implementação (código)
### 1) Suportar Apple no endpoint de OAuth
- Atualizar [route.ts](file:///Users/macmini/Documents/Projetos%20programação%20(trae)/App%20IronTracks/src/app/auth/login/route.ts) para aceitar `?provider=apple` (mantendo `google` como fallback).
- Resultado: a mesma rota `/auth/login` passa a servir tanto Google quanto Apple, controlado por querystring.

### 2) Adicionar botão “Continuar com Apple” na tela de login
- Atualizar [LoginScreen.js](file:///Users/macmini/Documents/Projetos%20programação%20(trae)/App%20IronTracks/src/components/LoginScreen.js):
  - Criar `handleAppleLogin()` que navega para `/auth/login?provider=apple&next=/dashboard`.
  - Exibir o botão no modo `login` (junto do login por e-mail), seguindo o visual do app.

### 3) Garantir que a conta demo não fica presa em aprovação
- Ajustar [page.tsx](file:///Users/macmini/Documents/Projetos%20programação%20(trae)/App%20IronTracks/src/app/wait-approval/page.tsx) para:
  - Se `user.email === 'apple-test@irontracks.com.br'`, redirecionar direto para `/dashboard`.

## Ajustes fora do código (para você fazer no Supabase)
- Em **Auth → Users**: marcar o usuário `apple-test@irontracks.com.br` como **Confirmado** (evita qualquer fluxo de OTP/confirm-email).
- Em **profiles**: garantir que existe linha para esse user e que o acesso não depende de aprovação (o bypass cobre o caso de cair na tela).

## Validação e entrega
- Rodar build e validar o fluxo: Apple OAuth → callback → dashboard.
- Enviar para a branch de deploy e preparar texto curto para colar nas “Review Notes”.
