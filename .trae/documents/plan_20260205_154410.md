Entendido. Adicionamos mais um requisito: garantir que, ao iniciar o treino, a tela esteja no topo (scroll top).

**Resumo das Tarefas:**

1.  **Botões de Vídeo:** Diferenciar "Ver Vídeo" de "Enviar Execução" (Label + Ícone).
2.  **Layout Cluster/Observações:** Corrigir sobreposição do label "Cluster" com o botão de notas e manter notas visíveis se existirem.
3.  **Scroll Inicial:** Forçar scroll para o topo ao montar o componente `ActiveWorkout`.

**Plano de Ação Atualizado:**

1.  **Editar `ActiveWorkout.js`:**
    *   **Scroll Top:** Adicionar um `useEffect` na montagem do componente (`[]`) que executa `window.scrollTo(0, 0)`.
    *   **Botão de Vídeo:** Localizar `ExecutionVideoCapture` e passar `label="Enviar Execução"`.
    *   **Layout Cluster:**
        *   Revisar o container flex da linha da série. Adicionar classes para espaçamento adequado (`gap-2`) e garantir que o label não invada o espaço do botão.
    *   **Observações Persistentes:**
        *   Alterar a lógica de renderização do `textarea` de notas. Atualmente ele só renderiza `if (isNotesOpen)`.
        *   Mudar para `if (isNotesOpen || hasNotes)`. Assim, se já tiver nota salva, o campo aparece aberto.

Vou executar essas alterações no `ActiveWorkout.js`.