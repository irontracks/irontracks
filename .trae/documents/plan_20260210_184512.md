Você tem toda razão! Peço desculpas. Ao implementar a trava de segurança, acabei bloqueando você (o Admin) também, o que não deveria ter acontecido.

Vou corrigir isso agora mesmo para que **Admins e Professores** entrem direto, e apenas **Usuários Alunos novos** passem pela tela de aprovação.

### O que aconteceu:

A trava que eu coloquei no [layout.tsx](file:///Users/macmini/Documents/Projetos%20programa%C3%A7%C3%A3o%20\(trae\)/App%20IronTracks/src/app/\(app\)/layout.tsx) estava olhando apenas se `is_approved` era verdadeiro ou falso, sem checar se você era o administrador do sistema.

### O plano de correção (Passo a passo):

**1. Ajustar o bloqueio de tela**
Vou alterar o arquivo [layout.tsx](file:///Users/macmini/Documents/Projetos%20programa%C3%A7%C3%A3o%20\(trae\)/App%20IronTracks/src/app/\(app\)/layout.tsx) para que ele faça o seguinte:

* Se o e-mail for o seu e-mail de admin (`djmkapple@gmail.com`), **libera na hora**.

* Se o cargo do usuário for `admin` ou `teacher`, **libera na hora**.

* Somente se for um usuário comum (`user`), ele checa se está aprovado.

**2. Garantir que Admins antigos já comecem aprovados**
Vou criar uma pequena migração no banco de dados para garantir que qualquer pessoa que já tenha o cargo de `admin` ou `teacher` no sistema hoje já fique com a marca de `is_approved = true` automaticamente.

***

### Resumo técnico do que vou mudar:

No arquivo `src/app/(app)/layout.tsx`, o código vai ficar assim:

```javascript
// ... busca o usuário e o cargo ...
const { role } = await resolveRoleByUser(user);

// Se for Admin ou Professor, entra direto!
if (role === 'admin' || role === 'teacher') {
  return children;
}

// Se for aluno (user), aí sim checa a aprovação
if (profile?.is_approved !== true) {
  redirect('/wait-approval');
}
```

Dessa forma, o seu acesso de Admin volta ao normal imediatamente e o sistema continua seguro para os novos alunos. Posso prosseguir com essa correção? <mccoremem id="01KH4BK07S82ZPQFBVJC0N5ANK" />
