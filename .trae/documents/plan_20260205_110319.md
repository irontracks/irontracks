Confirmado! O `ActiveWorkout.js` já está preparado para entender "SST" se a configuração vier correta.

Então, meu plano é focado totalmente no **Editor**:

1.  **Adicionar o Seletor:** Vou inserir o menu (Normal / Drop Set / SST) em cada série.
2.  **Configuração Automática:**
    *   Ao escolher **SST**, ele já vai preencher a configuração interna com `type: 'sst'`, ativando a lógica que já existe no sistema.
    *   O painel de configuração (que hoje mostra "Mini-sets", "Pausa", etc.) vai aparecer automaticamente para você ajustar os detalhes do SST.

Vou fazer essa alteração no `ExerciseEditor.js` agora. Assim que salvar, o treino ativo vai reconhecer e mostrar o timer e a etiqueta "SST" corretamente.