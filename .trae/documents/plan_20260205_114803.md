O problema é que a rota de API (`invite-candidates`) está definindo manualmente `lastSeen: null` para todos os alunos da lista, ignorando o dado real do banco de dados.

**Plano de Correção:**

1.  **Editar API (`src/app/api/team/invite-candidates/route.ts`):**
    *   Após buscar a lista de alunos na tabela `students`, farei uma segunda busca na tabela `profiles` usando os IDs desses alunos.
    *   Vou cruzar os dados para preencher corretamente os campos `lastSeen` (Visto por último) e `photoURL` (Foto de perfil), que hoje estão nulos.
    
Isso fará com que o status "Online" e o tempo "Visto por último" apareçam corretamente na modal de convite, pois o sistema de "Ping" (que atualiza o status online) já está funcionando corretamente no backend.

**Observação:**
Como a correção anterior (criar tabela `access_requests`) foi solicitada e aprovada, mas interrompida pela sua nova mensagem, vou incluir a execução dela neste turno também para garantir que o sistema fique íntegro.

1. Executar SQL para criar tabela `access_requests` (correção pendente).
2. Aplicar correção no código da API de convites (nova solicitação).