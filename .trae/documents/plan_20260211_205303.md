Para atender à solicitação de ter contadores visíveis e contextuais em **todos** os recursos VIP, vou atualizar os três componentes principais:

1. **WorkoutWizardModal.tsx** (Wizard de Treino):

   * Adicionar um indicador explícito de créditos na **Etapa 5** (Preview), logo acima dos botões de ação ("Gerar Treino").

   * Exibir: `Seus créditos: X/Y`.

2. **CoachChatModal.js** (Coach IA):

   * Adicionar um contador discreto mas visível na área de input (rodapé), para que o usuário saiba quantas mensagens restam enquanto digita.

   * Exibir: `Mensagens hoje: X/Y`.

3. **WorkoutReport.js** (Insights Pós-Treino):

   * Adicionar o contador junto ao botão "Gerar Insights" ou "Regerar", reforçando o custo da ação.

   * Exibir: `(X/Y créditos)`.

**Arquivos Afetados:**

* `src/components/dashboard/WorkoutWizardModal.tsx`

* `src/components/CoachChatModal.js`

* `src/components/WorkoutReport.js`

Essa abordagem garante que, independentemente de onde o usuário esteja (Chat, Relatório ou Wizard), ele terá visibilidade clara do saldo de créditos no momento da ação.

**Passos:**

1. Editar `WorkoutWizardModal.tsx` para inserir o bloco

