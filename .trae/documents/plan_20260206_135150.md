O problema visual ("duas palavras uma em cima da outra") e o erro de carregamento que você relatou no botão "POSTAR NO IRONTRACKS" (que na verdade você disse que é no "BAIXAR / COMPARTILHAR", mas a imagem mostra um erro visual no botão amarelo) parecem ser causados por um conflito de estado no componente durante o processamento do vídeo.

O texto sobreposto no botão amarelo (`POSTAR NO IRONTRACKS` + `PROCESSANDO...`) indica que o estado `busy` está `true`, mas o texto original não está sendo ocultado corretamente ou há uma renderização duplicada.

Além disso, se "não baixa o vídeo", significa que a Promise do `renderVideo` está travando ou falhando silenciosamente, deixando o app num estado "ocupado" (busy) eterno.

### Plano de Correção:

1.  **Corrigir Texto Sobreposto no Botão:** Ajustar a lógica de renderização condicional do texto do botão para garantir que, quando `busy` for true, apenas "PROCESSANDO..." apareça.
2.  **Destravar o Download:** Adicionar um timeout de segurança e logs de erro mais robustos no `renderVideo` e `createImageBlob` para garantir que, se o processo de renderização falhar ou demorar demais, o estado `busy` seja resetado e o usuário receba um feedback, em vez de ficar travado para sempre.
3.  **Reforçar Limpeza:** Garantir que o `cleanup` do `renderVideo` seja chamado mesmo em casos de erro inesperado.

Vou aplicar essas correções no `src/components/StoryComposer.tsx`.
