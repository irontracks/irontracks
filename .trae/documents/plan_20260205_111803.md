Vou resolver o erro de build e aplicar a melhoria do timer.

### O Problema do Build (Unexpected token)
O erro acontece porque o arquivo `src/app/api/ai/muscle-map-week/route` está escrito como TypeScript (tem anotações de tipo `(raw: any)`), mas não tem a extensão `.ts` ou `.tsx`. O Next.js/Webpack tenta ler como JavaScript puro e falha nos dois pontos.

**Solução:**
1.  Renomear o arquivo para `route.ts`.

### Melhoria do Timer (Sticky)
Como planejado, vou ajustar o cabeçalho do `ActiveWorkout.js` para garantir que o timer fique sempre visível e legível.

**Plano de Ação:**
1.  Renomear `src/app/api/ai/muscle-map-week/route` -> `route.ts`.
2.  Editar `ActiveWorkout.js`:
    *   Aumentar `z-index` para 50.
    *   Adicionar fundo semi-transparente (`bg-neutral-950/95` + `backdrop-blur`).
    *   Garantir `shrink-0` no relógio.

Posso executar?