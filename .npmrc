# Resolve peer dependency conflicts between @types/react@19 and @types/react-dom@18.
# Needed because @types/react-dom@^18.x declares peer @types/react@^18,
# but this project uses @types/react@19 (published directly by the React team).
# The conflict is nominal — the types are compatible at runtime.
legacy-peer-deps=true
